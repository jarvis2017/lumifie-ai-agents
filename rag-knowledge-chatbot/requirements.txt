# Third-party runtime dependencies. Install the shared `lumifie-core` separately:
#   uv pip install -e ../lumifie-core
# then:  uv pip install -r requirements.txt   (or: uv pip install -e ".[dev]")
#
# Optional extras (NOT installed here, kept out of CI/dev):
#   uv pip install -e ".[st]"   # sentence-transformers (semantic embeddings, pulls torch)
#   uv pip install -e ".[ui]"   # gradio (browser UI)
litellm>=1.80
pydantic>=2.6
chromadb>=0.5
pypdf>=4.2
python-docx>=1.1
beautifulsoup4>=4.12
httpx>=0.27
fastapi>=0.110
uvicorn>=0.30
loguru>=0.7
tenacity>=8.2
